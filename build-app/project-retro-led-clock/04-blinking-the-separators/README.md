# 目标 4：闪烁分隔符

## 说明

* main.go 文件包含前一步骤的解决方案
* "solution" 文件夹包含此步骤的解决方案

## 挑战步骤

分隔符应该每两秒显示一次。

### 示例：

* 第 1 秒：分隔符不可见
* 第 2 秒：分隔符可见
* 第 3 秒：分隔符不可见
* 第 4 秒：分隔符可见

### 提示

有两种实现方法：

1. 直接操作时钟数组（通过添加/移除分隔符）

2. 或者在打印时钟时决定要打印的占位符