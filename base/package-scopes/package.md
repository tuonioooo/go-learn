# Go 包（Package）声明完整教程

## 目录
1. [包的基本概念](#1-包的基本概念)
2. [包的作用](#2-包的作用)
3. [包的命名规则](#3-包的命名规则)
4. [包的组织结构](#4-包的组织结构)
5. [包的导入](#5-包的导入)
6. [导出与非导出](#6-导出与非导出)
7. [常见问题](#7-常见问题)

---

## 1. 包的基本概念

### 1.1 什么是包？

**包（Package）**是 Go 语言中组织代码的基本单位。

```go
// file1.go
package myapp

func HelloWorld() {
    println("Hello")
}

// file2.go
package myapp

func Goodbye() {
    println("Goodbye")
}
```

**特点**：
- `file1.go` 和 `file2.go` 属于同一个包 `myapp`
- 它们在同一个文件夹中
- 可以互相调用函数（即使是私有函数）

### 1.2 包 vs 文件夹

```
myapp/                    ← 文件夹
├─ main.go               ← package myapp
├─ handler.go            ← package myapp
├─ store.go              ← package myapp
└─ subdir/               ← 子文件夹（不同的包）
   ├─ helper.go          ← package subdir
   └─ util.go            ← package subdir
```

**关键**：
- 同一文件夹内的所有 `.go` 文件必须属于同一个包
- 不同文件夹可以是不同的包
- 子文件夹自动成为新包

---

## 2. 包的作用

### 2.1 代码组织

包将相关的代码组织在一起。

```go
// store/file_system_store.go
package store

type FileSystemPlayerStore struct {
    database *json.Encoder
    league   League
}

func (f *FileSystemPlayerStore) GetLeague() League {
    // ...
}

// store/memory_store.go
package store

type InMemoryPlayerStore struct {
    data map[string]int
}

func (i *InMemoryPlayerStore) GetLeague() League {
    // ...
}

// 两个文件都属于 store 包
// 可以互相访问彼此的函数和类型
```

### 2.2 命名空间隔离

不同包的代码相互隔离。

```go
// package A
type Player struct {
    Name string
}

// package B
type Player struct {
    ID int
}

// 两个 Player 类型不冲突！
// 使用时：A.Player 或 B.Player
```

### 2.3 访问控制

- **导出**（public）：大写字母开头 → 其他包可访问
- **非导出**（private）：小写字母开头 → 仅包内可访问

```go
package store

// ✅ 导出函数，其他包可以调用
func GetPlayerScore(name string) int {
    return 0
}

// ❌ 非导出函数，仅 store 包内可调用
func privateHelper() {
    // ...
}
```

---

## 3. 包的命名规则

### 3.1 合法的包名

```go
// ✅ 合法
package main
package myapp
package store
package handler
package poker
package api
package utils
package db_handler
package api2

// ❌ 非法
package Main         // 不能大写开头
package my-app       // 不能有连字符
package 123app       // 不能以数字开头
package my app       // 不能有空格
package func         // 不能是关键字
package 我的应用      // 不能包含中文
```

### 3.2 包命名最佳实践

| 原则 | 例子 | 说明 |
|------|------|------|
| 简短清晰 | `http`, `json`, `store` | 避免冗长 |
| 小写字母 | `server`, `database` | Go 约定 |
| 单数形式 | `user` (不是 `users`) | Go 约定 |
| 不重复 | `store.Store` (不是 `store.StoreStore`) | 避免冗余 |
| 意义明确 | `filepath` (不是 `path`) | 清楚表达用途 |

---

## 4. 包的组织结构

### 4.1 简单项目结构

```
myproject/
├─ go.mod                ← 模块定义
├─ main.go              ← package main
├─ handler.go           ← package main
├─ store.go             ← package main
└─ readme.md
```

**特点**：所有文件都在根目录，都属于 `main` 包。

### 4.2 中等项目结构

```
myproject/
├─ go.mod
├─ main.go              ← package main
├─ api/
│  ├─ handler.go        ← package api
│  └─ router.go         ← package api
├─ store/
│  ├─ file_system.go    ← package store
│  └─ memory.go         ← package store
└─ models/
   ├─ player.go         ← package models
   └─ league.go         ← package models
```

**结构**：
- `main` 包：程序入口
- `api` 包：HTTP 处理
- `store` 包：数据存储
- `models` 包：数据模型

### 4.3 复杂项目结构（标准库式）

```
myproject/
├─ go.mod
├─ main.go              ← package main
├─ cmd/
│  └─ server/
│     └─ main.go        ← package main
├─ internal/            ← 私有包（不能被其他项目导入）
│  ├─ handler/
│  │  └─ http.go        ← package handler
│  └─ store/
│     └─ file.go        ← package store
├─ pkg/                 ← 公共包（可以被其他项目导入）
│  └─ models/
│     └─ player.go      ← package models
└─ api/
   └─ handler.go        ← package api
```

---

## 5. 包的导入

### 5.1 基础导入

```go
package main

import (
    "fmt"                    // 标准库
    "encoding/json"
    "myproject/api"          // 你的项目包
    "myproject/store"
)

func main() {
    // 使用 fmt 包的函数
    fmt.Println("Hello")
    
    // 使用自定义包中导出的函数
    api.HandleRequest()
    store.GetPlayers()
}
```

### 5.2 导入的不同方式

```go
// 方式 1：标准导入
import "fmt"
import "encoding/json"

// 方式 2：分组导入（推荐）
import (
    "encoding/json"
    "fmt"
    
    "myproject/api"
    "myproject/store"
)

// 方式 3：别名导入（包名冲突时）
import (
    fmtpkg "fmt"
    json2 "encoding/json"
)

func main() {
    fmtpkg.Println("Hello")
}

// 方式 4：隐藏包名（不推荐）
import . "fmt"

func main() {
    Println("Hello")  // 不需要前缀
}

// 方式 5：只执行初始化（不使用包的功能）
import _ "database/sql/driver"
```

### 5.3 导入路径规则

```go
// 相对于项目根目录的 go.mod

// 项目结构
myproject/
├─ go.mod          ← module myproject
├─ api/
│  └─ handler.go   ← package api
└─ store/
   └─ file.go      ← package store

// 在 main.go 中导入
import "myproject/api"      // ✅ 正确
import "api"                 // ❌ 错误
import "./api"               // ❌ 错误（不支持相对路径）
```

---

## 6. 导出与非导出

### 6.1 导出规则

**大写字母开头 = 导出（Public）**
**小写字母开头 = 非导出（Private）**

```go
package store

// ✅ 导出 - 其他包可以使用
type Player struct {
    Name string      // ✅ 导出字段
    Wins int         // ✅ 导出字段
    secret string    // ❌ 非导出字段，包外不可访问
}

// ✅ 导出函数
func GetLeague() []Player {
    return nil
}

// ❌ 非导出函数
func getFromFile() []Player {
    return nil
}

// ✅ 导出常量
const MaxPlayers = 100

// ❌ 非导出常量
const minPlayers = 1
```

### 6.2 导出的实际效果

```go
// store/league.go
package store

type League struct {
    Players []Player
}

type Player struct {
    Name string
}

// main.go
package main

import "myproject/store"

func main() {
    // ✅ 可以使用导出的类型
    league := store.League{}
    
    // ❌ 不能使用非导出的字段
    league.players  // 编译错误
    
    // ✅ 可以使用导出的字段
    player := store.Player{Name: "Alice"}
}
```

### 6.3 包内访问

```go
// store/league.go
package store

type League struct {
    Players []Player
    secret string       // 非导出字段
}

func (l League) Find(name string) *Player {
    return l.find(name)  // ✅ 可以调用非导出函数
}

func (l League) find(name string) *Player {  // 小写，非导出
    // ...
}

// main.go
package main

import "myproject/store"

league := store.League{}
league.secret = "test"  // ❌ 编译错误，不可访问
league.find("Alice")    // ❌ 编译错误，不可访问
```

---

## 7. 常见问题

### 7.1 为什么包名会显示红线？

**原因**：VS Code 或编译器找不到包

**解决**：
1. 确保 `go.mod` 存在
2. 包路径与文件夹结构匹配
3. 检查是否在同一个包内混用了不同的包声明

```go
// ❌ 错误的混合
// file1.go
package main

// file2.go (同一文件夹)
package myapp  // ❌ 错误！应该用 main

// ✅ 正确
// file1.go
package main

// file2.go
package main  // 相同的包名
```

### 7.2 如何修改包名？

如果所有文件都需要改包名：

```go
// 原来
package main

// 改为
package poker
package api
package store
```

**重要**：同一文件夹内的所有 `.go` 文件必须用同一个包名。

### 7.3 什么时候用 main 包？

```go
// ✅ main 包用于可执行程序
package main

func main() {  // 程序入口
    // ...
}

// ❌ 库包不用 main
package mylib

// 库包没有 main() 函数
func SomeFunction() {
    // ...
}
```

### 7.4 internal 包有什么特殊性？

```
myproject/
├─ go.mod           ← module myproject
├─ pkg/
│  └─ models/       ← 可以被其他项目导入
└─ internal/
   └─ store/        ← 只能被 myproject 内部使用
```

```go
// 其他项目可以导入
import "myproject/pkg/models"

// 其他项目不能导入
import "myproject/internal/store"  // ❌ 编译错误
```

---

## 8. 最佳实践

### 8.1 包的设计原则

1. **单一职责**：每个包做一件事
   ```go
   package store   // 只处理数据存储
   package api     // 只处理 HTTP
   package models  // 只定义数据模型
   ```

2. **清晰的接口**：导出必要的，隐藏实现
   ```go
   // 导出接口
   type PlayerStore interface {
       GetScore(name string) int
   }
   
   // 隐藏实现细节
   type fileSystemStore struct {  // 小写，非导出
       database *json.Encoder
   }
   ```

3. **避免循环导入**
   ```go
   // ❌ 不能这样
   // package a 导入 package b
   // package b 导入 package a
   
   // ✅ 应该这样
   // package a 导入 package b
   // package b 不导入 package a
   ```

### 8.2 常见的包结构

```
项目/
├─ cmd/              ← 可执行程序
│  └─ main/
│     └─ main.go
├─ internal/         ← 私有包
│  ├─ handler/
│  └─ store/
├─ pkg/              ← 公共包（可被其他项目导入）
│  ├─ api/
│  └─ models/
└─ go.mod
```

### 8.3 包名命名建议

| 场景 | 包名 | 不好的名字 |
|------|------|-----------|
| 数据存储 | `store` | `database`, `db_manager` |
| HTTP 处理 | `api`, `http` | `handler_service` |
| 数据模型 | `models` | `types`, `entities` |
| 工具函数 | `util`, `helper` | `utils`, `helper_functions` |
| 配置管理 | `config` | `configuration`, `settings` |

---

---

## 9. 作用域（Scope）完整指南

### 9.1 什么是作用域？

**作用域（Scope）** 是指在代码中**声明的名称（变量、函数、常量等）的可见性范围**。

```go
package awesome

var enabled bool          // 包级作用域

func block() {
    var counter int       // 函数块作用域
    fmt.Println(counter)
}
```

### 9.2 Go 中的三个作用域

#### 1. 文件作用域（File Scope）

**仅在该文件内可见**，无法在同一包的其他文件中访问。

```go
// file1.go
package main

import "fmt"   // 文件作用域 - 只能在 file1.go 中使用

func main() {
    fmt.Println("Hello")  // ✅ 可以使用 fmt
}

// file2.go
package main

// fmt.Println("Hello")  // ❌ 编译错误，fmt 在这个文件中不可见
// 如果要在 file2.go 中使用，需要再 import 一次
```

**文件作用域的元素**：
- 导入的包名：`import "fmt"`
- 导入时的别名：`import f "fmt"`

#### 2. 包级作用域（Package Scope）

**在同一包的所有文件中可见**。

```go
// file1.go
package main

var name = "Alice"      // 包级作用域
const version = "1.0"   // 包级作用域

func Hello() {          // 包级作用域（导出）
    println("Hello")
}

func internal() {       // 包级作用域（非导出）
    println("internal")
}

// file2.go
package main

func main() {
    println(name)       // ✅ 可以访问，包级作用域
    println(version)    // ✅ 可以访问，包级作用域
    Hello()            // ✅ 可以调用
    internal()         // ✅ 可以调用（同一包内）
}
```

**关键点**：
- 同一包内的所有文件可以互相访问彼此的包级名称
- 无论函数、变量、常量是否导出，包内都可以访问
- 其他包只能访问导出的名称（大写开头）

#### 3. 块级作用域（Block Scope）

**仅在声明所在的块内可见**（函数内、if/for 内等）。

```go
package main

var declareMeAgain = 10  // 包级作用域

func nested() {
    // 块级作用域开始（在 nested 函数内）
    
    var declareMeAgain = 5  // 块级作用域，遮蔽（shadow）包级变量
    
    println(declareMeAgain)  // 输出：5
    
    if true {
        var localVar = 100   // 更深层的块级作用域
        println(localVar)     // ✅ 可以访问：100
    }
    
    // println(localVar)      // ❌ 编译错误，localVar 不在此作用域
}

func main() {
    println(declareMeAgain)  // 输出：10（访问包级变量）
    nested()
}
```

### 9.3 作用域的嵌套关系

```
┌─────────────────────────────────────┐
│        包级作用域（Package Scope）    │
│  包内所有文件都可以访问              │
│                                     │
│  ┌─────────────────────────────────┐│
│  │   文件作用域（File Scope）        ││
│  │   仅该文件可以访问（import 等）   ││
│  │                                 ││
│  │  ┌──────────────────────────────┐│
│  │  │  块级作用域（Block Scope）     │││
│  │  │  仅函数/块内可以访问          │││
│  │  │                              │││
│  │  │  内层可以访问外层的名称        │││
│  │  │  外层不能访问内层的名称        │││
│  │  └──────────────────────────────┘│
│  └─────────────────────────────────┘│
└─────────────────────────────────────┘
```

### 9.4 实际代码示例

```go
// awesome/main.go
package awesome

import "fmt"  // 文件作用域

var enabled bool  // 包级作用域

func block() {    // 包级作用域（导出）
    var counter int  // 块级作用域
    fmt.Println(counter)
}

// awesome/other.go
package awesome

func doSomething() {
    println(enabled)    // ✅ 可以访问，包级作用域
    fmt.Println("hi")   // ❌ 编译错误，fmt 是文件作用域
    block()            // ✅ 可以调用，包级作用域
}
```

### 9.5 作用域规则总结

| 作用域类型 | 可见范围 | 例子 |
|----------|--------|------|
| **文件作用域** | 仅该文件 | `import "fmt"` |
| **包级作用域** | 同一包的所有文件 | 在函数外声明的变量 |
| **块级作用域** | 声明所在的块内 | 在函数内声明的变量 |

### 9.6 名称遮蔽（Shadowing）

在内层作用域声明与外层相同的名称，会遮蔽外层的名称。

```go
package main

var x = 10  // 包级作用域

func test() {
    var x = 20  // 块级作用域，遮蔽包级的 x
    
    println(x)  // 输出：20
    
    {
        var x = 30  // 更深层的块级作用域
        println(x)   // 输出：30
    }
    
    println(x)  // 输出：20
}

func main() {
    println(x)  // 输出：10
    test()
}
```

**关键点**：
- 内层可以遮蔽外层的名称
- 遮蔽不是修改，只是在内层创建了新的名称
- 应该避免过度遮蔽，会造成代码混乱

---

## 10. 导入重命名（Import Renaming）

### 10.1 为什么需要导入重命名？

有时包名冲突或者包名太长，需要给导入的包起别名。

```go
package main

import (
    "fmt"
    f "fmt"              // 别名导入
    json1 "encoding/json"
    json2 "github.com/json/v2"  // 处理两个同名的 json 包
)

func main() {
    fmt.Println("Hello")    // 使用原名
    f.Println("Hi")         // 使用别名
}
```

### 10.2 导入重命名的用途

#### 1. **处理包名冲突**

```go
import (
    json1 "encoding/json"
    json2 "github.com/other/json"  // 两个都提供 json 包
)

func main() {
    // 使用别名区分
    data1 := json1.Marshal(obj1)
    data2 := json2.Marshal(obj2)
}
```

#### 2. **简化长包名**

```go
import (
    "github.com/very/long/package/name"  // 太长
)

// 改用别名
import (
    pkg "github.com/very/long/package/name"  // 简化
)

func main() {
    pkg.SomeFunction()  // 简洁
}
```

#### 3. **明确包的用途**

```go
package main

import (
    formatpkg "fmt"     // 明确这是 formatting 包
    f "fmt"             // 简写别名
)

func main() {
    formatpkg.Println("formatted output")
    f.Println("short alias")
}
```

### 10.3 常见的导入重命名模式

```go
package main

import (
    // 标准库
    "fmt"
    f "fmt"              // 简写
    
    // 长包名简化
    log "github.com/sirupsen/logrus"
    
    // 处理冲突
    stdJSON "encoding/json"
    extJSON "github.com/json/extended"
    
    // 仅初始化
    _ "github.com/lib/pq"  // PostgreSQL 驱动
)

func main() {
    f.Println("Using alias")
    log.Info("Logging")
    
    data1 := stdJSON.Marshal(obj1)
    data2 := extJSON.Marshal(obj2)
}
```

### 10.4 特殊的导入模式

```go
package main

import (
    // 1. 直接导入包的内容（不推荐）
    . "fmt"
    
    // 2. 仅初始化，不使用
    _ "database/sql/driver"
)

func main() {
    Println("No package prefix needed")  // 因为用了 . 导入
}
```

### 10.5 导入别名最佳实践

```go
// ❌ 不好的别名
import f "fmt"          // 太模糊
import h "github.com/handler/pkg"  // 不清楚

// ✅ 好的别名
import (
    "fmt"                      // 常用包，用原名
    format "fmt"               // 需要强调用途时
    logrus "github.com/sirupsen/logrus"  // 保留原包名
    stdJSON "encoding/json"    // 区分不同实现
    extJSON "github.com/json/extended"
)
```

---

## 11. 完整的学习题库及答案

### 11.1 包的存储位置

**Q: 包的源代码文件应该存储在哪里？**

1. 每个文件应该进入不同的目录
2. 在单个目录中 ✅ **正确**

**解释**：
- 同一包的所有文件必须在同一目录中
- 不同目录自动成为不同的包
- 一个文件夹只能对应一个包

```go
// ✅ 正确
myproject/
├─ api/
│  ├─ handler.go    // package api
│  └─ router.go     // package api
└─ store/
   ├─ file.go       // package store
   └─ memory.go     // package store

// ❌ 错误
myproject/api/
├─ handler.go  // package api
├─ router.go   // package other (不同的包！)
```

---

### 11.2 包声明的作用

**Q: 在 Go 源代码文件中，为什么使用包声明？**

1. 它用于导入包
2. 它用于告诉 Go 该文件属于某个包 ✅ **正确**
3. 它用于声明新函数

**解释**：
- `package` 声明告诉编译器这个文件属于哪个包
- `import` 语句是用来导入包的
- `func` 语句是用来声明函数的

```go
package main              // 包声明
import "fmt"              // 导入声明
func HelloWorld() {       // 函数声明
    fmt.Println("Hello")
}
```

---

### 11.3 包声明的位置

**Q: 包声明应该放在 Go 源代码文件的什么位置？**

1. 作为 Go 源代码文件中的第一段代码 ✅ **正确**
2. 作为 Go 源代码文件中的最后一段代码
3. 可以放在任何地方

**解释**：
- 包声明必须是文件的第一行代码（除了注释）
- 这是 Go 的语法要求
- 其他声明（import、func 等）都在包声明之后

```go
package main            // ✅ 必须第一个

import "fmt"            // 之后是导入

func main() {           // 然后是其他声明
    fmt.Println("Hi")
}
```

---

### 11.4 单文件中包声明的次数

**Q: 一个 Go 源代码文件中可以使用多少次包声明？**

1. 一次 ✅ **正确**
2. 零次
3. 多次

**解释**：
- 每个文件只能属于一个包
- 因此只能有一个包声明
- 同一文件夹内的所有文件必须有相同的包声明

```go
// ✅ 正确
package main
func Hello() {}

// ❌ 错误（不能出现两次）
package main
package main
func Hello() {}

// ❌ 错误（同一文件夹不能混用包名）
// file1.go
package main

// file2.go (同一文件夹)
package api  // 错误
```

---

### 11.5 正确的包声明语法

**Q: 以下哪个是正确的包声明用法？**

1. `my package`
2. `package main` ✅ **正确**
3. `pkg main`

**解释**：
- `package` 是关键字，必须写在前面
- 包名必须是有效的标识符
- 包名必须是单个单词（无空格）

```go
// ✅ 正确
package main
package api
package store
package poker

// ❌ 错误
my package      // 空格
Package main    // 大写
package 123     // 以数字开头
package my-pkg  // 特殊字符
```

---

### 11.6 同包文件的相互访问

**Q: 以下说法是否正确？**

"同一包的所有文件不能相互调用彼此的函数"

**答案**：✅ **错误**

正确说法：同一包的所有文件**可以**相互调用彼此的函数

**解释**：
- 同一包内的文件形成一个整体
- 任何一个文件都可以访问同一包中其他文件的包级（非导出）名称
- 即使是小写的私有函数，同一包也可以调用

```go
// bye.go
package main

func bye() {
    fmt.Println("Bye!")
}

// main.go (同一包)
package main

func main() {
    bye()  // ✅ 可以调用，即使 bye 是小写
}

// other_package/file.go (不同包)
package other

// bye()  // ❌ 不能调用，因为 bye 不是导出的
```

---

### 11.7 运行多个 Go 文件

**Q: 如何运行多个 Go 文件？**

1. `go run *.*go`
2. `go build *go`
3. `go run go`
4. `go run *.go` ✅ **正确**

**解释**：
- `go run *.go` 运行当前目录的所有 `.go` 文件
- 或指定具体文件：`go run file1.go file2.go file3.go`
- 或运行整个包：`go run ./` 或 `go run .`

```bash
# ✅ 正确的用法
go run main.go bye.go
go run *.go
go run ./
go run .

# ❌ 错误
go run *.*go    # 不支持这种模式
go build *go    # go build 不用于开发运行
```

---

### 11.8 包的类型

**Q: 以下哪个是正确的 Go 包类型？**

- 空包
- 可执行包 ✅ **正确**
- 可转移包
- 图书馆员包

**解释**：
Go 有两种包：
1. **可执行包（Executable Package）**：`package main` 且有 `func main()`
2. **库包（Library Package）**：其他包名，可被导入使用

```go
// 可执行包
package main

func main() {
    // 程序入口
}

// 库包
package api

func HandleRequest() {
    // 可被其他包导入使用
}
```

---

### 11.9 go run 和 go build 的区别

**Q: `go run` 可以执行的包类型？**

**答案**：可执行包 ✅

**解释**：
- `go run` 仅用于执行 `main` 包
- `go build` 可以编译任何包（可执行包和库包）

```bash
# ✅ 可以 run - main 包
go run main.go

# ❌ 不能 run - 库包
go run api/handler.go  # 编译错误

# ✅ 可以 build
go build main.go        # 生成可执行文件
go build api/handler.go # 编译库，不生成可执行文件
```

---

### 11.10 可执行包的标识

**Q: 以下哪个是可执行包？**

- `package main` 和 `func main` ✅ **正确**
- `package Main` 和 `func Main`
- `package exec` 和 `func exec`

**解释**：
- 可执行包必须命名为 `main`（小写）
- 必须有 `main()` 函数（小写）
- Go 用这个标记程序入口

```go
// ✅ 可执行包
package main

func main() {
    fmt.Println("Program starts here")
}

// ❌ 不是可执行包
package Main  // 包名大写错误
func Main() { // 函数名大写错误
    // ...
}
```

---

### 11.11 库包的标识

**Q: 以下哪个是库包？**

- `package main`
- `package lib` ✅ **正确**
- `func package`
- `package main` 和 `func main`

**解释**：
- 库包是除了 `main` 以外的任何包名
- 库包可以被其他包导入
- 库包没有 `main()` 函数

```go
// ✅ 库包
package mylib

func Helper() string {
    return "I can be imported"
}

// ❌ 不是库包（这是可执行包）
package main

func main() {
    // This is executable, not a library
}
```

---

### 11.12 包的用途选择

**Q: 哪种包类型用于可执行的 Go 程序？**

**答案**：可执行包（Executable Package）✅

**解释**：
- 创建程序（CLI、Web Server 等）用可执行包
- 创建库（被其他项目使用）用库包

```go
// 用于程序
package main

func main() {
    // 应用逻辑
}

// 用于库
package models

type User struct {
    Name string
}
```

---

### 11.13 可复用性与导入

**Q: 哪种包用于可复用性并且可以被导入？**

**答案**：库包（Library Package）✅

**解释**：
- 库包可以被其他项目导入和使用
- `main` 包不能被导入（特殊包）
- 库包提供公共接口和功能

```go
// ✅ 可以导入库包
import "myproject/models"
import "github.com/other/package"

// ❌ 不能导入 main 包
import "myproject/main"  // 编译错误
```

---

### 11.14 作用域的定义

**Q: 什么是作用域？**

- 可执行的代码块
- 声明名称的可见性范围 ✅ **正确**
- 确定要运行的内容

**解释**：
作用域决定了在代码的哪些位置，某个名称（变量、函数、常量）是可见和可访问的。

```go
package main

var x = 10  // 包级作用域，在整个包内可见

func test() {
    var y = 20  // 块级作用域，仅在 test() 函数内可见
    println(x)   // ✅ 可以访问 x
    println(y)   // ✅ 可以访问 y
}

func main() {
    println(x)   // ✅ 可以访问 x（包级）
    println(y)   // ❌ 不能访问 y（不在 test() 块内）
}
```

---

### 11.15 包级作用域的标识

**Q: 以下代码中，哪个名称是包级作用域？**

```go
package awesome

import "fmt"

var enabled bool

func block() {
    var counter int
    fmt.Println(counter)
}
```

选项：
1. `awesome`
2. `fmt`
3. `enabled` ✅ **正确**
4. `counter`

**解释**：
- `awesome`：包名，不是名称声明
- `fmt`：导入的包，文件作用域
- `enabled`：在函数外声明，包级作用域 ✅
- `counter`：在函数内声明，块级作用域

```go
package awesome

var enabled bool  // ✅ 包级 - 在函数外
var other int     // ✅ 包级 - 在函数外

func block() {
    var counter int  // ❌ 块级 - 在函数内
}
```

---

### 11.16 文件作用域的标识

**Q: 以下代码中，哪个名称是文件作用域？**

```go
package awesome

import "fmt"

var enabled bool

func block() {
    var counter int
    fmt.Println(counter)
}
```

选项：
1. `awesome`
2. `fmt` ✅ **正确**
3. `enabled`
4. `block()`
5. `counter`

**解释**：
- `fmt`：导入的包，只在本文件可见，文件作用域 ✅
- 其他导入的包名也是文件作用域
- 同一包的其他文件看不到这个导入

```go
// file1.go
package awesome

import "fmt"

func hello() {
    fmt.Println("file1")  // ✅ 可以使用 fmt
}

// file2.go
package awesome

func world() {
    fmt.Println("file2")  // ❌ 编译错误！fmt 不在此文件
    // 需要在 file2.go 中重新 import "fmt"
}
```

---

### 11.17 块级作用域的标识

**Q: 以下代码中，哪个名称在 `block()` 函数的块级作用域内？**

```go
package awesome

import "fmt"

var enabled bool

func block() {
    var counter int
    fmt.Println(counter)
}
```

选项：
1. `awesome`
2. `fmt`
3. `enabled`
4. `block()`
5. `counter` ✅ **正确**

**解释**：
- `counter` 在 `block()` 函数内声明，块级作用域 ✅
- 只能在 `block()` 函数内访问
- 函数外的代码无法访问它

```go
func block() {
    var counter int = 0
    println(counter)  // ✅ 可以访问
    
    if true {
        var inner = 5
        println(counter)  // ✅ 可以访问外层变量
        println(inner)    // ✅ 可以访问
    }
    
    println(inner)     // ❌ 错误，inner 在 if 块内
}

func main() {
    println(counter)   // ❌ 错误，counter 在 block() 内
}
```

---

### 11.18 作用域的访问规则

**Q: `block()` 函数可以看到 `enabled` 名称吗？**

```go
package awesome

import "fmt"

var enabled bool

func block() {
    var counter int
    fmt.Println(counter)
}
```

选项：
1. 是的，它在包级作用域 ✅ **正确**
2. 不，它在文件作用域
3. 不，它在 `block()` 的块级作用域

**解释**：
- `enabled` 是包级作用域（在函数外声明）
- 同一包内的所有代码都可以访问它
- 包括 `block()` 函数内的代码

```go
func block() {
    println(enabled)      // ✅ 可以访问包级变量
    println(counter)      // ✅ 可以访问块级变量
}

func anotherFunc() {
    println(enabled)      // ✅ 任何函数都可以访问包级变量
}
```

**访问规则**：
- ✅ 内层可以访问外层的名称
- ❌ 外层不能访问内层的名称

---

### 11.19 其他文件访问规则

**Q: `awesome` 包中的其他文件可以看到 `counter` 名称吗？**

```go
package awesome

import "fmt"

var enabled bool

func block() {
    var counter int
    fmt.Println(counter)
}
```

选项：
1. 是的
2. 不，它在包级作用域
3. 不，它在文件作用域
4. 不，它在 `block()` 的块级作用域 ✅ **正确**

**解释**：
- `counter` 在 `block()` 函数内（块级作用域）
- 只有 `block()` 函数内的代码可以访问它
- 其他文件无法访问（无论同包还是不同包）
- 即使是 `awesome` 包的其他文件也看不到

```go
// file1.go
package awesome

func block() {
    var counter int = 10
}

// file2.go (同一包)
package awesome

func test() {
    println(counter)  // ❌ 编译错误！看不到 block() 内的 counter
}
```

---

### 11.20 导入包的作用域

**Q: `awesome` 包的其他文件可以看到 `fmt` 名称吗？**

```go
package awesome

import "fmt"

var enabled bool

func block() {
    var counter int
    fmt.Println(counter)
}
```

选项：
1. 是的
2. 不，它在包级作用域
3. 不，它在文件作用域 ✅ **正确**
4. 不，它在块级作用域

**解释**：
- `fmt` 是导入的包，文件作用域
- 仅在该文件内可见
- 其他文件要使用 `fmt`，必须在自己的文件中再次导入

```go
// file1.go
package awesome

import "fmt"

func hello() {
    fmt.Println("file1")  // ✅ 可以使用
}

// file2.go (同一包)
package awesome

// 没有 import "fmt"

func world() {
    fmt.Println("file2")  // ❌ 编译错误
    // 必须添加 import "fmt"
}
```

---

### 11.21 同一作用域中的重声明

**Q: 在同一作用域中重复声明相同名称会发生什么？**

```go
package awesome

var enabled bool
var enabled bool  // 在包级作用域中重新声明

func block() {
    var counter int
    fmt.Println(counter)
}
```

选项：
1. 新声明会覆盖旧的
2. 不能这样做。它已在包级作用域中声明 ✅ **正确**
3. 不能这样做。它已在文件作用域中声明

**解释**：
- 在同一作用域中不能重复声明相同的名称
- 否则编译器无法知道你想要哪一个
- 这会导致编译错误："redeclared"

```go
package main

var x = 10
var x = 20  // ❌ 编译错误：redeclared in this block

// ✅ 可以在不同的作用域使用相同名称
var x = 10

func test() {
    var x = 20  // ✅ 不同的作用域，可以
    println(x)   // 输出：20
}

func main() {
    println(x)   // 输出：10
}
```

---

### 11.22 不同作用域中的名称遮蔽

**Q: 在不同的作用域中声明相同名称会发生什么？**

```go
package awesome

var enabled bool  // 包级作用域

func block() {
    var enabled bool  // 块级作用域
    
    var counter int
    fmt.Println(counter)
}
```

选项：
1. 新声明会覆盖旧声明 ✅ **正确**
2. 不能这样做。它已在包级作用域中声明
3. 不能这样做。它已在文件作用域中声明

**解释**：
- 可以在内层作用域声明与外层相同的名称
- 这叫"遮蔽"（shadowing）
- 在内层作用域中，新名称会遮蔽外层的名称
- 两个变量都存在，但在内层只能访问到内层的

```go
package awesome

var enabled bool = true  // 包级

func block() {
    var enabled bool = false  // 块级，遮蔽包级
    
    println(enabled)  // 输出：false（块级的）
}

func anotherFunc() {
    println(enabled)  // 输出：true（包级的）
}

func main() {
    block()         // 输出：false
    anotherFunc()   // 输出：true
}
```

**关键要点**：
- ✅ 可以在内层遮蔽外层的名称
- ✅ 它们都能共存
- ✅ 外层变量仍然完好无损
- ⚠️ 应该避免过度遮蔽，会造成代码混乱

---

## 总结

### 包的核心概念

| 概念 | 说明 |
|------|------|
| **包声明** | 文件顶部的 `package name` |
| **同一包** | 同文件夹内的所有 `.go` 文件属于同一包 |
| **导出** | 大写字母开头，其他包可访问 |
| **非导出** | 小写字母开头，仅包内可访问 |
| **导入** | `import "path/to/package"` |

### 作用域的核心概念

| 作用域 | 可见范围 | 例子 |
|------|--------|------|
| **文件作用域** | 仅该文件 | 导入的包：`import "fmt"` |
| **包级作用域** | 同一包的所有文件 | 函数外的变量：`var x = 10` |
| **块级作用域** | 声明所在的块内 | 函数内的变量：`var x = 20` |

### 访问规则速记

```
┌─────────────────────────────────────┐
│  同一包的其他文件                    │
│  ✅ 可以访问包级名称                 │
│  ✅ 可以调用包级函数（即使私有）     │
│  ❌ 不能访问块级变量                 │
│  ❌ 不能访问其他文件的 import        │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  其他包                              │
│  ✅ 可以访问导出的名称（大写）        │
│  ❌ 不能访问非导出名称（小写）       │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  块内的代码                          │
│  ✅ 可以访问块内变量                  │
│  ✅ 可以访问外层作用域的变量          │
│  ❌ 不能访问其他块内的变量            │
└─────────────────────────────────────┘
```

### 快速检查清单

- [ ] 同一文件夹内的包名是否一致？
- [ ] 导出函数/类型是否首字母大写？
- [ ] 导入路径是否正确？
- [ ] 是否有循环导入？
- [ ] 是否避免过度遮蔽？
- [ ] 同一作用域是否有重复声明？

这就是 Go 包和作用域的完整介绍！🎯
